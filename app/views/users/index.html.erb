<h1>All Users</h1>
<ul>
    <% @users.each do |user| %>
        <li>
           <%= link_to user.name, user %> (friends: <%= user.friends.count %>) 
           <%= link_to user.short_url, user.short_url %>
        </li>
    <% end %>
</ul>